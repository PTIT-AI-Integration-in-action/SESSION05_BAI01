# BÀI 1: Phân tích & Lựa chọn - Thiết kế mô tả Tool (Metadata Description) tối ưu

## Bối cảnh

Trợ lý AI đặt phòng cần cấu hình siêu dữ liệu mô tả (Metadata Description) cho hai công cụ `getRoomAvailability` và `calculateTotalPrice` trong Spring AI. If mô tả không đủ rõ ràng, model AI sẽ nhầm lẫn giữa hai công cụ và gọi sai hàm hoặc truyền thiếu tham số.

## Phương án lựa chọn: Phương án B

## Phân tích phương án chọn

Tôi chọn **Phương án B** vì nó là phương án duy nhất mô tả công cụ theo góc nhìn khách hàng người dùng, tức là theo "ý định nhiệm vụ" (intent) và "input/output" của tool, đúng thứ mà cơ chế Function Calling của mô hình LLM thực sự tiêu thụ.

Khi Spring AI đăng ký một tool, nó sinh ra JSON Schema mô tả tool đó (tên, mô tả, danh sách tham số và kiểu dữ liệu) rồi gửi vào prompt của model. Model dựa vào description để quyết định xem nên gọi tool nào và truyền tham số gì. Description là kênh duy nhất mà model có để hiểu "công cụ này để làm gì" và "cần những gì". Điều đó giải thích vì sao chất lượng mô tả quyết định trực tiếp độ chính xác của việc gọi hàm.

Phương án B đáp ứng đầy đủ bốn tiêu chí của một description tốt:

1. Trả lời câu hỏi "Khi nào dùng tool này (when)". Nó nói rõ `calculateTotalPrice` chỉ được gọi sau khi đã xác định được loại phòng và số ngày. Điều này giúp model sắp xếp đúng thứ tự gọi tool chứ không gọi bừa.

2. Trả lời câu hỏi "Tool nhận tham số gì (what input)". Nó liệt kê tên tham số, kiểu dữ liệu và cả định dạng chuẩn. Ví dụ khai báo checkInDate và checkOutDate ở định dạng `yyyy-MM-dd`, roomType có ví dụ cụ thể Deluxe/Standard. Có ví dụ cụ thể làm model trục xuất tham số chính xác hơn nhiều so với chỉ nêu tên.

3. Trả lời câu hỏi "Tool trả về gì (what output)". Nó nói rõ `getRoomAvailability` trả về trạng thái và đơn giá mỗi đêm. Biết trước output giúp model dùng kết quả để nuôi tool tiếp theo (tool chaining).

4. Trả lời câu hỏi "Có điều kiện hay ràng buộc gì (constraints)". Nó ghi rõ `numberOfDays > 0`, đây là ràng buộc nghiệp vụ quan trọng mà model phải tuân theo.

Nhờ mô tả tường minh, hai công cụ có "signature" ngữ nghĩa khác hẳn nhau: một cái là "kiểm tra tình trạng phòng" (trả về availability + price per night), một cái là "tính tổng chi phí" (nhận roomType + numberOfDays, trả về double). Model không thể nhầm lẫn khi cả mục đích, tham số và định dạng đều được tách bạch.

## Phân tích các phương án loại trừ

### Phương án A (Mô tả tối giản) - loại trừ

- Rủi ro lớn nhất là nhầm lẫn giữa hai tool. Cả hai mô tả "Check phòng trống khách sạn" và "Tính toán giá tiền phòng" quá ngắn, không nêu tham số bắt buộc. Embedding ngữ nghĩa của hai tool có thể gần nhau trong không gian vector, khiến model không phân biệt được khi nào dùng công cụ nào.
- Không nêu định dạng ngày `yyyy-MM-dd`, nên model tự do truyền bất kỳ chuỗi nào như "15/07/2026", "ngày mai", gây DateTimeParseException ở tầng Java.
- Không nêu tham số bắt buộc (checkInDate, checkOutDate, roomType), nên model hay truyền thiếu tham số, kéo theo lỗi hoặc gọi sai. Thiếu ràng buộc `numberOfDays > 0` còn gây ra tính toán sai nghiệp vụ.
- Với các model có ngân sách context nhỏ hoặc hoạt động kém với câu lệnh mơ hồ, việc thiếu thông tin này càng làm tăng xác suất suy diễn sai.

Nói cách khác Phương án A gây đúng các nhóm lỗi mà đề bài cảnh báo: nhầm hàm, thiếu tham số, sai định dạng ngày.

### Phương án C (Mô tả kỹ thuật nội bộ) - loại trừ

- Mô tả tập trung vào chi tiết triển khai: tên class, tên bảng MySQL `room_status`, cơ chế JPA, kiểu trả về double. Những chi tiết này hoàn toàn vô nghĩa với model, vì model không thể (và không cần) truy cập database hay hiểu nội bộ JPA.
- Model cần biết "nghiệp vụ gì", chứ không phải "hàm gì". Cái mô tả kỹ thuật này không giúp model quyết định tham số cần truyền, định dạng ngày ra sao, hay khi nào nên gọi. Nó gần như vô dụng trong việc trục xuất tham số.
- Chi tiết "kết nối vào bảng room_status" thậm chí còn gây hại: nó rò rỉ danh sách chi tiết nội bộ ra prompt, làm tăng số token không cần thiết trong context (tốn chi phí) và khiến mô tả thiếu focus vào mục đích sử dụng.
- Còn tệ hơn khi mô tả tiết lộ cách tính giá là "tích đơn giá nhân số ngày" nhưng bỏ qua ràng buộc `numberOfDays > 0` và yêu cầu định dạng ngày, dẫn đến cùng nhóm lỗi như Phương án A.
- Phiên bản C cũng không phân biệt được rõ ràng mục đích của hai tool: cả hai đều được mô tả theo kiểu "thực thi hàm X", nên model dễ mất phương hướng trong việc chọn tool.

Kết luận: Phương án C mô tả đúng cái máy tính hiểu nhưng sai thứ model cần. Description tốt phải nói theo ngôn ngữ nghiệp vụ của người dùng, đầy đủ tham số và định dạng, đúng như Phương án B đã làm.